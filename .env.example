# Example environment variables for deployment
# Frontend: set this to your deployed backend URL (no trailing slash)
VITE_API_URL=https://your-backend.example.com

# Backend: example PORT (Render/Heroku set this automatically)
PORT=4000
